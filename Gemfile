source "https://rubygems.org"

# Ruby 4.x doesn't include a bunch of stuff that 3.x did...
gem "base64"
gem "bigdecimal"
gem "csv"

# This will help ensure the proper Jekyll version is running.
gem "jekyll", "~> 4.3"

group :jekyll_plugins do
  gem 'jekyll-paginate'
  gem 'jekyll-sitemap'
end

